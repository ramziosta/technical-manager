import React from 'react';
import Loader from '../components/Loader/index';

const Home = () => {
  const item = { color: "red", height: "25px"}
  return (
    <div>
      {/* <Loader color={item.color} height={item.height} /> */}

    </div>
  );
};

export default Home;
